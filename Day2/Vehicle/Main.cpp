#include "Vehicle.h"
#include "Type.h"

#include "Functionalities.h"

/*
a)developer makes container of object and container is copied 
when  create object is caalled from createobject function to
main function 
B)user creates an empty container user passes its reference to function 
developer accesses this reference puts the object directly in that container



*/

int main(){
    //size 3 
    //vehicle * it points 
    Vehicle* arr[3];
    //parameter> address of first location 
    CreateObjects(arr);

}
